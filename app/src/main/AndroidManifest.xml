<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.xjst.skysweety">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".SkySweetyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".test.eventbus.TestBoardCastActivity"
            android:label="@string/title_activity_test_board_cast"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"/>
        <activity android:name=".test.TestToastActivity"/>
        <activity android:name=".test.eventbus.ActivityB">
        </activity>
        <activity android:name=".test.TestLoadViewActivity">
        </activity>
        <activity
            android:name=".test.eventbus.ActivityA">
        </activity>
    </application>

</manifest>